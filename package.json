{
  "name": "fate-symphony",
  "version": "1.0.0",
  "description": "A project for audio synthesis and MIDI parsing.",
  "main": "fate.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "echo \"No linting configured\" && exit 0",
    "build": "echo \"No build step required\" && exit 0"
  },
  "author": "russellcooper",
  "license": "MIT"
}
